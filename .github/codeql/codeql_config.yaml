name: "Custom codeql configuration"

disable-default-queries: true

queries:
  - name: Extended-Security
    uses: security-extended
  - name: Security and Quality
    uses: security-and-quality

paths:
  - examples/transaction-import

languages:
  - python
